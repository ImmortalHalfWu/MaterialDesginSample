<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wuimmortalhalf.activitysmaterialdesginsample.materialdesginsample">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- CoordinatorLayoutActivity -->
        <!-- CollapsingToolbarLayoutActivity -->
        <!-- StatusBarTestActivity -->
        <!-- ScrollingActivity -->
        <!--ScrollingActivity2-->
        <activity
            android:name=".activitys.ScrollingActivity2"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar.ScrolingActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".activitys.MainActivity" -->
        <!-- android:label="@string/title_activity_main" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".activitys.CollapsingToolbarLayoutActivity" -->
        <!-- android:label="@string/title_activity_main" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->


        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".activitys.ScrollingActivity" -->
        <!-- android:label="@string/title_activity_scrolling" -->
        <!-- android:theme="@style/AppTheme.NoActionBar"></activity> -->


        <!-- <activity android:name=".activitys.StatusBarTestActivity"></activity> -->
        <!--<activity-->
            <!--android:name=".activitys.ScrollingActivity2"-->
            <!--android:label="@string/title_activity_scrolling2"-->
            <!--android:theme="@style/AppTheme.NoActionBar"></activity>-->
    </application>

</manifest>